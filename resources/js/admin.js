/**
 * Example javascript file.
 *
 * @since 0.1.0
 */

import { someRandomFunction } from './functions';

document.addEventListener( 'DOMContentLoaded', () => {
	// eslint-disable-next-line no-console
	console.log( someRandomFunction() );
} );
